using UnityEngine;

public class RandomCubes : MonoBehaviour
{
    //Checks
    int i ;

    //gameobejct
    public GameObject Scube, MCube,LCube;

    // Update is called once per frame
    void Update()
    {
        //Checking
        // if(Input.GetKeyDown(KeyCode.Space))
        // {
        //     Vector3 randomSpawnPosition = new Vector3(Random.Range(-10, 11), 1.5, Random.Range(-10, 11));
        //     Instantiate(cubePrefab, randomSpawnPosition, Quaternion.identity);
        //     //Ok done sa check
        // }

    }

    void SpawnCubes()
    {
        
    }
}
